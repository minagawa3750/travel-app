<!DOCTYPE html>
<html>
  <head>
    <title>TravelApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class ="header_left_nav">
      <%= link_to image_tag("https://presite-potepancamp-rails-02.herokuapp.com/assets/logo.png"), root_path %>
      <%= form_with url: search_rooms_path, method: :get, local: true do |f| %>　　
        <%= f.search_field :address, placeholder: "エリアを検索", class:"header_search_field" %>
       <% end %>
       <%= form_with url: search_rooms_path, method: :get, local: true do |f| %>　　
         <%= f.search_field :keyword, placeholder: "キーワード", class:"header_search_field" %>
        <% end %>
      </div>
         <div class ="header_right_nav">
          <% if user_signed_in? %>　
           <li class="dropdown">
                <div class="header_user_name">
                  <%= current_user.user_name %>
                </div>
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  <% if current_user.user_image? %>
                   <%= image_tag current_user.user_image.url, :size => '50x50', class:"user_image" %>
                   <% else %>
                   <%= image_tag ('https://presite-potepancamp-rails-02.herokuapp.com/assets/default_icon-9263fc59c414b7228d256fc178dcb22183561357950a68f5ad086ba7ee054974.jpg'),
                     :size => '50x50', class:"user_image" %>
                  <% end %>
                  <span class="caret"></span></a>
                 <ul class="dropdown-menu">
                  <li><a class="dropdown-item" href="#"><%= link_to "ルーム登録", new_room_path %></a></li>
                  <li><a class="dropdown-item" href="#"><%= link_to "予約済みルーム一覧", reservations_path %></a></li>
                  <li><a class="dropdown-item" href="#"><%= link_to "登録済みルーム一覧", rooms_path %></a></li>
                  <li><a class="dropdown-item" href="#"><%= link_to "設定", users_account_path %></a></li>
                  <li><a class="dropdown-item" href="#"><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></a></li>
                 </ul>
               </li>
          <% else %>
              <%= link_to "ログイン", new_user_session_path, class: 'right_nav_1' %>
              <%= link_to "新規登録", new_user_registration_path, class: 'right_nav_2' %>
            </div>
          <% end %>

  </header>
  <% if flash[:notice] %>
     <p class="notice"><%= flash[:notice] %></p>
   <% end %>

    <%= yield %>

    <footer>
      © 2022 MINAPAN.INC.
    </footer>
  </body>
</html>
